{
  "name": "bobs-amazing-game-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=packages/game-engine && npm run build --workspace=client",
    "build:engine": "npm run build --workspace=packages/game-engine",
    "type-check": "npm run type-check --workspace=packages/game-engine && npm run type-check --workspace=client"
  },
  "devDependencies": {
    "typescript": "~5.7.3"
  }
}

