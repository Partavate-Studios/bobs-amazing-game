import { Component } from '../Component'

/**
 * Pushable component - entities that can be pushed (like crates)
 */
export class Pushable extends Component {
  constructor(entityId: number) {
    super(entityId)
  }
}

